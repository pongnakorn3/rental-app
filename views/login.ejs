<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เข้าสู่ระบบ</title>
    <style>
        body { font-family: 'Sarabun', sans-serif; display: flex; justify-content: center; padding-top: 50px; margin: 0; background-color: #f5f5f5; }
        .container { background: white; padding: 30px; width: 100%; max-width: 350px; height: 100vh; box-sizing: border-box; }
        
        h2 { text-align: center; margin-bottom: 30px; }
        
        /* ปรับ Label ให้สวยงามเหมือนหน้า Register */
        label { display: block; margin-bottom: 5px; font-weight: bold; font-size: 14px; color: #333; }
        
        input { width: 100%; padding: 15px; margin-bottom: 15px; background: #d9d9d9; border: none; border-radius: 5px; box-sizing: border-box; font-size: 14px; }
        
        /* ปรับปุ่มให้เป็นสีเข้ม ดูเด่นขึ้น */
        .btn { width: 100%; padding: 15px; background: #333; color: white; border: none; border-radius: 5px; cursor: pointer; font-size: 16px; margin-top: 10px; font-weight: bold; }
        .btn:hover { background-color: #555; }

        .back-btn { font-size: 24px; text-decoration: none; color: black; display: block; margin-bottom: 20px; }
        .text-center { text-align: center; margin-top: 20px; font-size: 14px; }
        a { color: black; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <a href="/" class="back-btn"><</a>
        <h2>เข้าสู่ระบบ</h2>
        
        <form action="/login" method="POST">
            <label>อีเมล หรือ เบอร์โทรศัพท์</label>
            <input type="text" name="username" required placeholder="กรอกอีเมล หรือ เบอร์โทรศัพท์">
            
            <label>รหัสผ่าน</label>
            <input type="password" name="password" required>
            
            <button type="submit" class="btn">เข้าสู่ระบบ</button>
            
            <div class="text-center" style="margin-top: 15px;">
                <a href="#" style="font-weight: normal; color: #666; font-size: 13px;">ลืมรหัสผ่าน?</a>
            </div>
        </form>

        <div class="text-center" style="margin-top: 50px;">
            ยังไม่มีบัญชี? <a href="/register">สมัครสมาชิก</a>
        </div>
    </div>
</body>
</html>